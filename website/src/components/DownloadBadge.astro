---
import { Image } from 'astro:assets';

const { url, alt, image } = Astro.props
---
<a href={url}>
    <Image
        class="w-50 md:w-65"
        src={image}
        alt={alt}
        width={646} 
        height={250}
        layout="constrained" />
</a>