package xyz.malkki.neostumbler.utils.geocoder

import xyz.malkki.neostumbler.R

enum class GeocoderType(val titleResId: Int) {
    DEFAULT(R.string.geocoder_default_title),
    DISABLED(R.string.geocoder_disabled_title),
}
